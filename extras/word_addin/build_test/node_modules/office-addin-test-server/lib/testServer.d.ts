export declare const defaultPort: number;
export declare class TestServer {
    private jsonData;
    private port;
    private testServerStarted;
    private app;
    private resultsPromise;
    private server;
    constructor(port: number);
    startTestServer(mochaTest?: boolean): Promise<boolean>;
    stopTestServer(): Promise<boolean>;
    getTestResults(): Promise<JSON>;
    getTestServerState(): boolean;
    getTestServerPort(): number;
    getPlatformName(): string;
    private startListening;
}
