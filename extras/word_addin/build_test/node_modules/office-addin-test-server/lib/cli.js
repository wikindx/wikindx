// Copyright (c) Microsoft Corporation. All rights reserved.
// Licensed under the MIT license.
Object.defineProperty(exports, "__esModule", { value: true });
const commander = require("commander");
const office_addin_cli_1 = require("office-addin-cli");
const commands = require("./commands");
const testServer_1 = require("./testServer");
commander.name("office-addin-test-server");
commander.version(process.env.npm_package_version || "(version not available)");
commander
    .command("start")
    .option(`-p --port [port number]", "Port number must be between 0 - 65535. Default: ${testServer_1.defaultPort}`)
    .action(commands.start);
// if the command is not known, display an error
commander.on("command:*", function () {
    office_addin_cli_1.logErrorMessage(`The command syntax is not valid.\n`);
    process.exitCode = 1;
    commander.help();
});
if (process.argv.length > 2) {
    commander.parse(process.argv);
}
else {
    commander.help();
}
//# sourceMappingURL=cli.js.map